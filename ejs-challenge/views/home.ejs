<%- include("partials/header"); -%>

<h1>Home</h1>
<p><%= homeContent %></p>

<% postList.forEach(function(post) {%>
    <div class="postItem">
        <h1><%= post.title %></h1>
        <p>
            <%= post.content.substr(0,100) + '...' %><a href="/posts/<%= post.title %>"> Read More</a>
        </p>
        
    </div>
<% }); %>

<%- include("partials/footer"); -%>